import React from "react";

const year = new Date().getFullYear();

function Footer() {
  return (
    <footer>
      <p>Note taking app with Django and React</p>
    </footer>
  );
}

export default Footer;
